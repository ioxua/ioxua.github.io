{
  "name": "aula7",
  "version": "1.0.0",
  "description": "- criar projeto com SASS/TS/Gulp - criar um crud de Users, com dashboard :)",
  "main": "index.js",
  "scripts": {
    "build": "gulp build:clean && gulp build:sass && gulp build:pug",
    "serve": "npm run build && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-pug": "^4.0.1",
    "gulp-sass": "^4.1.0",
    "node-sass": "^5.0.0",
    "static-server": "^2.2.1"
  }
}
